<?php 
$lolo=mysql_connect('localhost','root','','final scala');
if (insset($_POST['names'])) {
	# code...
	$target_dir="uploads/";
	$nm=rand(2000,100000);
	//moving the upload file to the folder called uploads
	$target_file=$target_dir . 4num.basename($FILES["fileToUpload"]["tmp_name"])
}
 ?>